<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>리스트</title>
</head>

<body>
    <a href="/">메인 페이지로!</a>
    <a href="/listlistlist">리스트 페이지로!</a>
    <a href="/insertinsertinsert">입력 페이지로!</a>
    <div>데이터 리스트임</div>
    <% data.forEach((a)=> { %>
        <div>
            <a href="/modifymodifymodify/<%= a.id %>">
                <%= a.id %>
                    <%= a.name %>
                        <%= a.number %>
            </a>
            <form action="/listlistlist/<%= a.id %>" method="post">
                <button>삭제</button>
            </form>
        </div>
        <% }) %>
</body>

</html>